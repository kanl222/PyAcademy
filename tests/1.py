import random
print(random.random())



def isPalindrome(list_:list[int]):
        """
        :type head: ListNode
        :rtype: bool
        """
        current_index = 0
        last_index = 1
        while current_index < last_index:
                while list_[current_index] != list_[last_index]:
                        last_index+=1
                list_[current_index] =  
        